import NutritionTab from "@/components/NutritionTab";

/* Onglet Nutrition = destination racine (barre du bas) → plein écran, sans
   bouton retour « ← Progression » (on n'y arrive plus depuis Progression). */
export default function NutritionPage() {
  return <NutritionTab />;
}
